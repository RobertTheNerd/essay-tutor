# Frontend Environment Variables Configuration
# Copy this file to .env.local and configure as needed

# Development Features
# Set to 'true' to show the test button in production builds
# If not set, the test button will only show in development mode (DEV=true)
VITE_SHOW_TEST_BUTTON=false

# API Configuration (if needed in future)
# VITE_API_BASE_URL=http://localhost:3000
# VITE_API_TIMEOUT=30000

# Feature Flags (if needed in future)
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_DEBUG_MODE=false
